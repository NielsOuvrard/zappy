name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: print env
      run: env
      env:
        CC: gcc-10
        CXX: g++-10
    - name: make
      run: make
      env:
        CC: gcc-10
        CXX: g++-10
    - name: make check
      run: make check
      env:
        CC: gcc-10
        CXX: g++-10
    - name: make distcheck
      run: make distcheck
      env:
        CC: gcc-10
        CXX: g++-10
